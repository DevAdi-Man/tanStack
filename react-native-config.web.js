const Config = {
  // Add whatever environment variables your app needs to run here!
  // For example, if your src/lib/api.ts file uses Config.API_URL, add it:
  // API_URL: "http://localhost:3000",
};

export default Config;
